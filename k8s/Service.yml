apiVersion: v1
kind: Service
metadata:
  name: k8stester-service
  labels:
    app: k8stester
spec:
  selector:
    app: k8stester
    tier: backend
  ports:
  - port: 80
    targetPort: 3000
  type:
    ClusterIP
